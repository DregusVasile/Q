"""Local configuration used when running scripts from `I/src`.

This mirrors values from `I/data/config.py` so scripts can import `config` directly.
"""

n = 11
nr_matrici = 100
target = 10000
"""Temporary local config placeholder (removed)."""

raise RuntimeError("Do not import this file; use data.config instead")
